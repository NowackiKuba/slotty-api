meta {
  name: Cancel Event
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/events/:eventId/cancel
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

params:path {
  eventId: {{eventId}}
}

body:json {
  {
    "byWho": "TRAINER"
  }
}

docs {
  byWho: TRAINER | CUSTOMER.
}
