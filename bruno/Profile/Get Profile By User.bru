meta {
  name: Get Profile By User
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/users/:userId/profile
  body: none
  auth: inherit
}

params:path {
  userId: {{userId}}
}

docs {
  Public lookup by user id. JWT still required by controller guard.
}
