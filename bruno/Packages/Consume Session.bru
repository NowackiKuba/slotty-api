meta {
  name: Consume Session
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/packages/{{customerPackageId}}/consume
  body: none
  auth: inherit
}

docs {
  POST /api/packages/:customerPackageId/consume
}
